services:
  eventLars:
    image: eventlars
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5173:5173
    volumes:
      - ${EVENTS_ROOT}:/events

networks: 
  default: 
    name: traefik 
    external: true