services:
  eventlars:
    image: eventlars
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - 5173:3000
    volumes:
      - ${EVENTS_ROOT}:/events

networks: 
  default: 
    name: traefik 
    external: true